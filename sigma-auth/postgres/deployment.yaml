apiVersion: apps/v1
kind: Deployment
metadata:
  name: sigma-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sigma-db
  template:
    metadata:
      labels:
        app: sigma-db
    spec:
      containers:
        - name: postgres
          image: postgres:17
          env:
            - name: POSTGRES_DB
              value: sigma_auth
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          emptyDir: {}
