apiVersion: apps/v1
kind: Deployment
metadata:
  name: ohio-order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ohio-order
  template:
    metadata:
      labels:
        app: ohio-order
    spec:
      containers:
        - name: ohio-order
          image: andrew4coding/ohio-order:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              value: jdbc:postgresql://ohio-db:5432/ohio_order
            - name: MENU_SERVICE_URL
              value: http://mewing-menu:8080
            - name: DATABASE_USERNAME
              value: postgres
            - name: DATABASE_PASSWORD
              value: postgres